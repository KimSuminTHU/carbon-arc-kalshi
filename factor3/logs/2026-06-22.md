# Factor 3 실험 로그 — 2026-06-22 · 킥오프 (설계·인프라 확정)

> 산출: `factor3/` 문서 일체 · 관련: `PLAN.md`, `DESIGN.md`, `DATA_PIPELINE.md`

## 목적
Factor 3(`{X,Z,LLM}→Y`) 실험을 본격 시작하기 위한 **설계 확정 + 인프라 정찰 + 문서 구조 수립**. 코드 실행(Phase 0~)은 다음 로그부터.

## 무엇을 했나
- **연구 프레이밍 확정**: X=카드(CA0056), Z=전분기 어닝콜, Y=매출 서프라이즈. RQ=X+Z LLM 결합이 단독 대비 보완적인가.
- **레포 파이프라인 정찰**: 35티커 목록·`ca0056_..._q_3y.csv`, Y 구성(`s_t_revsurprise_factset.py`, FactSet CONS_EARLY), 멀티팩터(`s_ac`), 검정유틸(`s_q_edge_tests`: surrogate/cluster_boot/oos_dm, `s_v`: dm_hln) 확인.
- **Z 접근 경로 확정**: linq MCP `stock_documents`로 `file_key` 조회 → S3 버킷 `REDACTED-S3-BUCKET`에서 `_corrected.html` 다운로드(credit-agent `download_from_s3` 재사용).
- **LLM 확정**: `gpt-5.5` (id는 `linq-meeting-agent` 확인), 호출=credit-agent `client.beta.chat.completions.parse(response_format, reasoning_effort)`, 키=credit-agent/meeting-agent `.env`(`OPENAI_API_KEY`/AWS). infra 기본은 `gpt-5.4-2026-03-05` → env로 `gpt-5.5` 강제 예정.
- **누설 규칙 확정**: gpt-5.5 cutoff **2025-12-01** → 테스트 = report>cutoff = 2025Q4~2026Q2 = **105 event**. 입력은 pre-print point-in-time.
- **수량 정정**: 테스트 105 ≠ 처리 transcript. 지도학습(A/C/N) 훈련용 pre-cutoff 포함 시 처리 ≈**350**(B-only면 105). 비용 ≈$100–250.
- **문서 구조 생성**: `README/PLAN/DESIGN/DATA_PIPELINE/RESULTS/RELATED_WORK` + `logs/`.
- **계획 승인 완료**(plan mode), 실행 권한 확보.
- **문헌 리뷰**: 워크플로 2회(`w8g3si1ti`, `wolos43m2`) 전 에이전트 API 529로 실패 → **메인 루프 인라인 WebSearch/WebFetch로 직접 수행, `RELATED_WORK.md` 작성(웹 검증)**. 핵심 선행(Lopez-Lira&Tang, Kim-Muhn-Nikolaev, Jha, ACL2023, Fleder-Shah, Clark-West, HLN, look-ahead 누설 문헌)·**novelty 확인**(교집합 비어 있음).

## Verdict
설계·인프라·문서 구조 확정. 실행 준비 완료(미실행).

## Caveats
- 35티커 전부 2023–2026 `_corrected.html` 커버리지 미확인 → Phase 0 첫 작업.
- gpt-5.5 실제 호출 가능성 스모크 미실시.
- 문헌(novelty 포함) 미확정 — 워크플로 대기.

## Next
- **Phase 0**: `s_ae_fetch_transcripts.py` 작성 → 스모크(MCD 1건 S3+gpt-5.5 1콜) → 35티커 커버리지 점검 → X/Y 패널과 정렬, 누설 가드 assert.
- 문헌 워크플로 완료 시 `RELATED_WORK.md` 작성 + novelty 위치.
